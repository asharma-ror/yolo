import React from "react"

import ResetPassword from "../../containers/account/resetPassword"

const PasswordReset = ({ location }: any) => (
  <ResetPassword location={location} />
)

export default PasswordReset
